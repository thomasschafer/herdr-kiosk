[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
herdr-kiosk = { path = ".." }
syn = { version = "2", features = ["full", "parsing"] }
toml = "0.9"

[lints]
workspace = true
