[package]
name = "xtask"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
anyhow = "1"
herdr-kiosk = { path = ".." }
syn = { version = "3", features = ["full", "parsing"] }
toml = "1.1"

[lints]
workspace = true
